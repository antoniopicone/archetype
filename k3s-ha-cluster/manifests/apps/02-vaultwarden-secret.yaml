apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secret
  namespace: apps
type: Opaque
stringData:
  # IMPORTANT: Change this password to match the one in database/03-patroni-secrets.yaml
  # Format: postgresql://username:password@host:port/database
  DATABASE_URL: "postgresql://vaultwarden:ChangeMe_VaultwardenDB123!@patroni.database.svc.cluster.local:5432/vaultwarden"
